services:
  - type: web
    name: null0dv-void-store
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
        optional: true
      - key: SUPABASE_URL
        value: https://iriuznaoxstqsepisldd.supabase.co
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false